export type Classification = {
    year: string,
    make: string,
    model: string,
}

export enum Colors {
    Gray,
    Green,
    Red,
    Blue,
    Silver,
    Black
}

export type FuelType = 'Gasoline' | 'Diesel';
